export type HeroProps = {
  title: string;
  subtitle: string;
  imageUrl: string;
};

export type NavigationItem = {
  label: string;
  href: string;
};

export type HeaderProps = {
  title: string;
  logoUrl?: string;
};